# Epics

- An epic is a human-facing grouping of beads
- Epics are optional and used for reporting / context
- Example structure in JSON:

{
  "id": "user-auth",
  "name": "User Authentication",
  "beads": ["backend-create-login", "frontend-login-ui"]
}
